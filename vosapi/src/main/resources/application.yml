spring:
  application:
    name: vos-api
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  datasource:
    url: jdbc:postgresql://13.203.31.80:5432/VOS
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

# AWS SNS Configuration
aws:
  region: ap-south-1
  sns:
    topic-arn: arn:aws:sns:us-east-1:123456789012:vos-notifications

# Application Configuration
app:
  base:
    url: http://localhost:8080
  invite:
    token:
      expiry:
        hours: 168  # 7 days
  otp:
    expiry:
      minutes: 10

# Logging
logging:
  level:
    com.vos: DEBUG
    org.springframework.security: DEBUG
    org.hibernate.SQL: DEBUG

# Server Configuration
server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always

ACCESS_KEY: AKIA6OLSRFCG3TW2WYFN
SECRET_KEY: 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

